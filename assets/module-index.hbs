/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const create = require('./create')
const {{pascalcase name}} = require('./{{snakecase name}}')
{{#each sub}}const {{@key}} = require('{{{this}}}')
{{/each}}

let lib = create.bind(this)

Object.assign(lib, {{pascalcase name}}, {
{{#each sub}}  {{@key}},
{{/each}}
  create,
  {{pascalcase name}}
})

module.exports = lib
