/**
 * Test case for {{spinalcase name}}.
 * Runs with mocha.
 */
'use strict'

import {{pascalcase name}} from '../lib/{{snakecase name}}'
import React from 'react'
import assert from 'assert'
import { shallow } from 'enzyme'

describe('{{spinalcase name}}', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = shallow(
      <{{pascalcase name}}/>
    )
    assert.ok(element)
  })
})

/* global describe, before, after, it */
